# ISSUE-8 输入性能实测指南

## 🎯 测试目标
验证输入延迟 p95 < 16ms，确保编辑器达到 Bear 级别的流畅度。

## 🛠 测试环境
- **开发服务器**: http://localhost:5173
- **浏览器**: Chrome/Firefox（推荐使用开发者工具）
- **测试时长**: 10 分钟连续输入

## 📋 测试步骤

### 1. 准备工作
1. 打开浏览器访问 http://localhost:5173
2. 打开开发者工具（F12）
3. 切换到 Console 标签页
4. 确保编辑器已加载完成

### 2. 重置性能数据
在 Console 中执行：
```javascript
// 点击 "Reset input perf" 按钮或
// 在控制台查看重置确认信息
```

### 3. 开始连续输入测试
1. 点击编辑器区域，确保光标在编辑器内
2. 进行 **10 分钟连续输入**，建议：
   - 输入正常文本内容
   - 包含中英文混合
   - 包含标点符号
   - 模拟真实写作场景
   - 保持稳定的输入节奏

### 4. 导出性能数据
10 分钟后，在 Console 中执行：
```javascript
// 点击 "Print input perf" 按钮
// 或直接在控制台查看输出的表格数据
```

### 5. 记录关键指标
从 Console 输出中记录：
- `count`: 总输入次数
- `p50`: 中位数延迟
- `p95`: 95分位数延迟 ⭐ **关键指标**
- `max`: 最大延迟

## ✅ 验收标准

### 性能指标
- [ ] **p95 延迟 < 16ms** ⭐ **核心要求**
- [ ] p50 延迟 < 8ms
- [ ] max 延迟 < 50ms

### 用户体验
- [ ] 光标无跳动
- [ ] 无异常滚动
- [ ] 输入流畅无卡顿

### 技术验证
- [ ] 输入过程中无 React 全量渲染
- [ ] 编辑器响应及时

## 🔧 React 渲染验证

1. 打开 React DevTools（如果已安装）
2. 切换到 "Components" 标签
3. 观察 "Highlight updates when components render"
4. 在输入过程中观察：
   - Editor 组件是否频繁重新渲染
   - 是否有无关组件的渲染

## 📊 性能数据记录模板

```
=== ISSUE-8 输入性能实测报告 ===
测试时间: YYYY-MM-DD HH:MM:SS
测试时长: 10 分钟
输入内容: [描述输入内容类型]

性能指标:
- 总输入次数: [count]
- p50 延迟: [p50]ms
- p95 延迟: [p95]ms ⭐
- 最大延迟: [max]ms

验收结果:
- [✅/❌] p95 < 16ms
- [✅/❌] 光标无跳动
- [✅/❌] 无异常滚动
- [✅/❌] 无 React 全量渲染

结论: [通过/未通过]
建议: [改进建议]
```

## 🚨 注意事项

1. **关闭其他标签页** - 避免浏览器资源竞争
2. **关闭不必要的应用** - 确保系统资源充足
3. **保持稳定输入** - 避免长时间停顿
4. **记录异常情况** - 如发现卡顿、跳动等立即记录

## 📞 问题排查

如果 p95 > 16ms：
1. 检查是否有后台任务干扰
2. 检查 IndexedDB 操作是否影响输入
3. 检查 React 组件渲染优化
4. 重新测试并对比数据

## 🎉 完成后

1. 将测试结果记录到 `performance-test-report.md`
2. 更新 `docs/ISSUES_AND_MILESTONES.md` 中的 ISSUE-8 状态
3. 如果通过，可以开始 ISSUE-9 崩溃恢复验证
