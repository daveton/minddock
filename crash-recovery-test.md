# ISSUE-9: 崩溃恢复验证测试方案

## 🎯 测试目标
验证浏览器崩溃后的恢复机制，确保用户数据不会丢失。

## 📋 当前恢复机制分析

### 现有实现
1. **自动保存** - 300ms debounce 保存到 IndexedDB
2. **内存缓存** - `noteCache` 存储当前 note
3. **持久化存储** - IndexedDB 存储所有 notes
4. **会话恢复** - `localStorage` 记录最后活跃的 note ID

### 恢复流程
1. 页面加载时调用 `ensureDefaultNote()`
2. 从 `localStorage` 获取最后活跃的 note ID
3. 从 IndexedDB 加载 note 内容
4. 恢复到编辑器中

## 🧪 测试场景

### 场景 1: 正常关闭重开
1. 编辑一些内容
2. 等待自动保存完成（300ms 后）
3. 正常关闭标签页
4. 重新打开
5. **预期**: 内容完整恢复

### 场景 2: 强制关闭重开
1. 编辑一些内容
2. 在自动保存完成前（< 300ms）强制关闭
3. 重新打开
4. **预期**: 恢复到上一个保存点

### 场景 3: 浏览器崩溃模拟
1. 编辑大量内容
2. 使用 `chrome://crash` 或任务管理器强制关闭浏览器
3. 重新打开
4. **预期**: 内容完整恢复

### 场景 4: 多次崩溃测试
1. 重复崩溃恢复 20 次
2. 统计恢复成功率
3. **预期**: 成功率 > 95%

## 🔧 测试工具

### 崩溃模拟方法
```javascript
// 方法 1: Chrome 崩溃页面
chrome://crash

// 方法 2: 无限循环导致页面卡死
while(true) {}

// 方法 3: 强制关闭浏览器进程
```

### 恢复时间测量
```javascript
// 在 EditorView.tsx 中添加时间测量
const recoveryStart = performance.now()
const note = await ensureDefaultNote()
const recoveryTime = performance.now() - recoveryStart
console.log(`Recovery time: ${recoveryTime}ms`)
```

## 📊 验收标准

### 功能验收
- [ ] 模拟崩溃后重新打开，内容可恢复
- [ ] 恢复成功率 > 95%
- [ ] 恢复时间 < 1 秒

### 数据完整性验收
- [ ] 文本内容完整无丢失
- [ ] 格式信息保持正确
- [ ] 多个 notes 都能正确恢复
- [ ] 最后活跃 note 正确恢复

### 用户体验验收
- [ ] 恢复过程无错误提示
- [ ] 恢复后可立即编辑
- [ ] 状态提示正确显示

## 🚀 实现步骤

### 步骤 1: 添加恢复时间测量
在 `ensureDefaultNote()` 中添加性能测量

### 步骤 2: 创建测试脚本
自动化崩溃恢复测试流程

### 步骤 3: 执行测试
运行 4 个测试场景，收集数据

### 步骤 4: 分析结果
统计成功率和恢复时间

## 📈 预期结果

### 成功率目标
- 正常关闭重开: 100%
- 强制关闭重开: 98%
- 浏览器崩溃: 95%
- 总体成功率: > 95%

### 恢复时间目标
- 平均恢复时间: < 500ms
- 最大恢复时间: < 1000ms

## 🔍 问题排查

如果恢复失败：
1. 检查 IndexedDB 是否正常工作
2. 检查 `localStorage` 是否可用
3. 检查自动保存是否触发
4. 检查错误处理逻辑

## 📝 测试报告模板

```
=== ISSUE-9 崩溃恢复验证报告 ===
测试时间: YYYY-MM-DD HH:MM:SS
测试环境: Chrome 版本 / 操作系统

测试结果:
- 正常关闭重开: X/X 成功
- 强制关闭重开: X/X 成功  
- 浏览器崩溃: X/X 成功
- 总体成功率: XX%

恢复时间:
- 平均恢复时间: XXXms
- 最大恢复时间: XXXms
- 最小恢复时间: XXXms

数据完整性:
- [✅/❌] 文本内容完整
- [✅/❌] 格式信息正确
- [✅/❌] 多 notes 恢复正常

结论: [通过/未通过]
问题: [发现的问题列表]
建议: [改进建议]
```

## 🎯 完成标准

1. 所有测试场景执行完毕
2. 恢复成功率 > 95%
3. 恢复时间 < 1 秒
4. 数据完整性验证通过
5. 生成完整测试报告

完成后可以进入 ISSUE-10 保存失败路径验证。
